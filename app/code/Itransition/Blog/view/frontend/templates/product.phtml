<?php
$product = $block->getPorductModel();
$posts = $product->getPosts();

foreach ($posts as $post): ?>
<ul>
    <li><a href="<?= $product->generateUrl($post->getIdentifier()) ?>"><?= $post->getTitle() ?></a></li>
</ul>
<?php endforeach; ?>
